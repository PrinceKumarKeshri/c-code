#include<stdio.h>
#include<conio.h>
void  main()
{
	clrscr();
	int count=0;
	while(int i=0,i<10,i++);    // why we can't do this
	{
		printf("i am a winner\n")   ;
		count++;
	}
	printf("\ntotal number of times hello print = %d",count);
	getch();
}