#include<stdio.h>
#include<conio.h>
void main()
{
	clrscr();
	char s1="p";     // we want to output  char but charachter is always assign in single qoutes
	// character constant must be as long one or two character wo do not write as like 'prince'
	printf("My name is %s",s1);
	getch();
}