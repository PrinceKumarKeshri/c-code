#include<stdio.h>
#include<conio.h>
void main()
{
	clrscr();
	int n,i,sum=0;
	printf("Enter n value = ");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		sum=sum+n; // if we add n instead of i then it will add n times to itself
	}
	printf("Sum of first %d numbers = %d",n,sum);
	getch();
}